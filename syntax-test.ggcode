// Regular double-slash comment - should be GREEN
// Another regular comment - should be GREEN

let radius = 10 /// @number 5 50 // Circle radius in mm
let center_x = 0 /// @number -100 100 // Center X coordinate  
let draw_circle = 1 /// @check // Draw the main circle
let pattern_type = 1 /// @selection 1, 2, 3, 4 // Pattern type
let filename = "test" /// @text // Output filename

// More regular comments - should be GREEN
G0 X[center_x] Y[center_y] // Move to center - should be GREEN
G1 X[center_x + radius] Y[center_y] F150 // Draw line - should be GREEN

// Final comment - should be GREEN

/* 
Expected behavior with new state-based tokenizer:
1. Regular // comments → GREEN (muted comment color)
2. /// @annotation → BLUE (configurator triple-slash)
3. @number, @check, etc. → PURPLE (annotation keywords)  
4. Parameter ranges → GREEN (numeric ranges)
5. // Description → ITALIC GREEN (description comments)
*/